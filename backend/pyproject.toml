[project]
name = "voxa-backend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "flask-cors",
    "psycopg2-binary",
    "bcrypt",
    "google-generativeai",
    "python-dotenv",
    "boto3",
    "gunicorn"
]